let phoneNumber ;

let carMarque , carModele , carMatricule , carPhoneNumber , carPassword;

var logged=true;

var cars = new Array() ;
var positions = new Array() ;
var previousCar=1;
var previousPosition=0;

let date , hour ,  latitude , logitude;

const options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
